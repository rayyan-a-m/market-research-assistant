fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
python-multipart==0.0.20

# LLM orchestration (versions resolved together on the langchain 0.3.x line)
langchain==0.3.30
langchain-core==0.3.86
langchain-google-genai==2.0.11   # Gemini: summarizer, judge, discovery, embeddings
# Second ChatProvider adapter, targeting any OpenAI-compatible endpoint. Not on
# the default path (the judge runs on Gemini); it exists so the provider seam is
# demonstrably real and so the judge can be pointed at another model family by
# config. See ProviderFactory.judge and DESIGN_DECISIONS.md #10.
langchain-openai==0.3.35
openai==1.109.1

# Fetching / extraction (Stage 1)
crawl4ai==0.4.247
pdfplumber==0.11.4               # text layer of an uploaded PDF
jellyfish==1.1.0                 # Jaro-Winkler title dedup in discovery

# Retrieval (in-memory cosine — see ARCHITECTURE.md)
numpy==2.2.1

# HTTP client (search providers + Clerk JWKS)
httpx==0.28.1

# Persistence (direct Postgres over the Supabase DATABASE_URL)
asyncpg==0.30.0

# Auth: verify Clerk session JWTs against Clerk's JWKS
pyjwt[crypto]==2.10.1
